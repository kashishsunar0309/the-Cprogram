#include <stdio.h>
#include <conio.h>
int main()
{
    int sum = 25, count = 5, mean1, x;
    float mean2;
    mean1 = sum / count;
    printf(" value of mean1 :%d\n", mean1);
    mean2 = mean1;
    printf("value of mean2 : %f\n", mean2);
    x = 7.5;
    printf("x=%d", x);
    getch();
}
